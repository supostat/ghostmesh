[package]
name = "ghostmesh-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
ghostmesh_core = { path = "../src-tauri" }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
tracing = "0.1"
tracing-subscriber = "0.3"
